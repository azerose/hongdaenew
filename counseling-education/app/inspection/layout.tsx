import Footer from '../_components/common/footer';
import Header from '../_components/common/header';

const InspectionLayout = ({ children }: { children: React.ReactNode }) => {
    return <>{children}</>;
};

export default InspectionLayout;
